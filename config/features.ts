export const features = {
  // Set to false to disable the highly unoptimized Spline Bot easily during dev/builds
  splineBot: true,
  // Other features
  guestbook: true,
  terminal: true,
  nowPlaying: true,
  blog: true,
  pageTransitions: true,
};
